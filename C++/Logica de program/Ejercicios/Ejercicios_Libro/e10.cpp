/*Ordenar una Lista de Strings: Crea un programa que reciba una lista de palabras (strings) y 
las ordene alfabéticamente usando el algoritmo de ordenación de tu elección 
(burbuja, inserción, etc.).*/

#include <iostream>
using namespace std;

string sortCadena(string cadena){
    string init;
    for(int i{0}; i < cadena.size(); i++){
        
    }
}